--github push codes 

# 1. See what changed
git status

# 2. Stage changes
git add .

# 3. Commit them
git commit -m "Your clear commit message"

# 4. Pull remote changes (optional but good)
git pull origin main

# 5. Push to GitHub
git push origin main